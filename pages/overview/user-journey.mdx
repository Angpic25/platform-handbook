---
title: Full User Journey
description: The most complete user journey in the platform, available to clients with at least one Gold touchpoint.
---

import { Steps, Callout } from 'nextra/components'

# Full User Journey

This is the most complete journey a client can experience within the platform. It covers every major feature, from the Home dashboard through to issue resolution and expert retest.

<Callout type="info">
To access the full journey shown here, the client must have at least one Gold touchpoint in their account. Some features — including the retest request and expert comments — are only available on the Gold tier.
</Callout>

<Steps>

### Land on the Home dashboard

After logging in, the client sees the company level overview with key metrics (active critical issues, resolved issues) and a list of highest impact touchpoints.

<video aria-hidden="true" tabIndex={-1} src="/images/video-home-dashboard-tour.mp4" autoPlay loop muted playsInline controls style={{ width: '100%', borderRadius: '8px' }} />

### Navigate to a Touchpoint overview

The client clicks on a touchpoint from the Home dashboard or from the Touchpoints list to see the detailed compliance dashboard.

<video aria-hidden="true" tabIndex={-1} src="/images/video-home-to-overview.mp4" autoPlay loop muted playsInline controls style={{ width: '100%', borderRadius: '8px' }} />

### Review the Issues list

From the Touchpoint overview, the client switches to the Issues tab to see all identified accessibility issues with filters, search, and sorting.

<video aria-hidden="true" tabIndex={-1} src="/images/video-issue-management.mp4" autoPlay loop muted playsInline controls style={{ width: '100%', borderRadius: '8px' }} />

### Open an Issue detail

The client clicks on an issue to open the side panel, which shows the Overview tab with full details, the Fix tab with suggested remediation, and the Jira tab for ticket linking.

<video aria-hidden="true" tabIndex={-1} src="/images/video-issue-detail-tabs.mp4" autoPlay loop muted playsInline controls style={{ width: '100%', borderRadius: '8px' }} />

### Create a Jira ticket

From the Jira tab in the issue detail, the client can link the issue to an existing Jira ticket or create a new one to assign the fix to a developer.

<video aria-hidden="true" tabIndex={-1} src="/images/video-jira-from-issue.mp4" autoPlay loop muted playsInline controls style={{ width: '100%', borderRadius: '8px' }} />

### Fix the issue and request a retest

After the development team implements the fix, the client marks the issue as Fixed and requests an expert retest to validate the remediation.

<video aria-hidden="true" tabIndex={-1} src="/images/video-fix-and-retest.mp4" autoPlay loop muted playsInline controls style={{ width: '100%', borderRadius: '8px' }} />

### Track progress over time

The client returns to the Touchpoint overview to monitor the trend chart and compliance metrics, verifying that the overall accessibility score improves over time.

![Touchpoint overview trend chart showing progress over time](/images/placeholder-trend-chart.png)

</Steps>